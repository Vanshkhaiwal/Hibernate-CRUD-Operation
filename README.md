This project demonstrates CRUD (Create, Read, Update, Delete) operations using the Hibernate ORM framework with a MySQL database. It showcases how Java objects can be mapped to relational tables, enabling seamless persistence and retrieval of data.


⚙️ Features
- Hibernate configuration with MySQL database
- Entity classes with annotations for mapping
- DAO layer for modular CRUD operations
- Integration with JSP/Servlets for user interface
- Transaction management and exception handling
- Queries using HQL and Criteria API


🛠️ Technologies Used
- Java
- Hibernate ORM
- MySQL
- JSP/Servlets
- Eclipse IDE
- Maven


🚀 How to Run
- Clone the repository
- Configure Hibernate with your MySQL database (update hibernate.cfg.xml)
- Import project in Eclipse/IntelliJ
- Run the application and test CRUD operations
